{% extends 'base.html.twig' %}

{% block title %}Size{% endblock %}

{% block body %}
    <h1>Size</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ size.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ size.label }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ size.slug }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ size.position }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_size_index') }}">back to list</a>

    <a href="{{ path('app_size_edit', {'id': size.id}) }}">edit</a>

    {{ include('admin/size/_delete_form.html.twig') }}
{% endblock %}
